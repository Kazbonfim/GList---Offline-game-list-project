<!doctype html>
<html lang="pt-BR">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Lista de Jogos</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="/stylesheets/visuals.css">
</head>

<body>
    <div class="container mt-4">
        <div class="text-center mb-4">
            <h2>Ambiente de testes (front)</h2>
            <div class="alert alert-warning" role="alert">
                Ambiente de testes, não recomendo alterar este documento, ou reutilizar ele no produto final
            </div>
        </div>

        <hr />

        <!-- Lista de jogos -->
        <div class="row">
            {{#each games}}
            <div class="col-6 col-md-4 col-lg-3 mt-2">

                {{!-- Card, COMEÇO --}}

                <div class="card">
                    <div class="card-header card-title game-title text-white bg-dark">{{this.title}}</div>
                    {{!-- Imagem --}}
                    <div class="image-container">
                        <img src="/images/placeholder.png" class="x" alt="Imagem do jogo: {{this.title}}"
                            id="image-{{this.id}}" />
                    </div>
                    {{!-- Dados do título --}}
                    <div class="card-body bg-secondary text-white">
                        <h5>Plataforma: <span>{{this.console}}</span></h5>
                        <h5>Gênero: <span>{{this.genre}}</span></h5>
                        <div hidden>
                            <!-- Rating -->
                            {{#each this.rating}}
                            <i class="bi bi-star-fill"></i>
                            {{/each}}
                        </div>
                    </div>
                    <div class="card-footer bg-dark">
                        <div class="dropdown show">
                            <a class="btn btn-sm btn-secondary dropdown-toggle" href="#" role="button"
                                id="dropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                Gerenciar
                            </a>
                            <div class="dropdown-menu" aria-labelledby="dropdownMenuLink">
                                <!-- Formulário para exclusão do jogo -->
                                <form class="remove-game-form" action="/games/deletegames/{{this.id}}" method="post">
                                    <button class="dropdown-item" type="submit">Deletar</button>
                                </form>
                                <a class="dropdown-item" href="#"><!-- Favoritar --></a>
                                <a class="dropdown-item" href="#"><!-- Visualizar --></a>
                            </div>
                        </div>
                    </div>

                    {{!-- Card, FIM --}}

                </div>
            </div>
            {{/each}}
        </div>
        <hr />

        <div class="mt-5">
            <a href="/" class="btn btn-primary">Voltar ao Início</a>
        </div>

        <!-- Scripts -->
        <script src="/javascripts/fetchGamesFront.js" defer></script>
        <script src="/javascripts/deleteGameAnimation.js" defer></script>
        <script src="https://code.jquery.com/jquery-3.5.1.min.js" defer></script>
        <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.16.0/umd/popper.min.js"></script>
        <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.bundle.min.js" defer></script>
</body>

</html>